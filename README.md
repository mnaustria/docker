# simple python flask docker

----


## Build Application

Build the docker image manually by cloning the git repository
> $ git clone https://github.com/mnaustria/docker.git
> $ docker build -t mnaustria/simple-python-flask .

